import { LoaderIcon } from "lucide-react";


const Loader = () => {
  return (
    <div>
      <LoaderIcon  size={20} className="animate-spin" />
    </div>
  );
};

export default Loader;
